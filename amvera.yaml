meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  skip: false
  additionalCommands: npm ci && npm run build
  artifacts:
    "*": /

run:
  command: npm run start
  containerPort: "3000"